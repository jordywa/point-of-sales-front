rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated users to read any document in any collection
    match /{document=**} {
      allow read: if request.auth != null;
      allow write: if false; // Keep write access denied for this example
    }
  }
}
